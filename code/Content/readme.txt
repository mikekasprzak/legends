This folder (Content) is special.

It is populated per-project by the content.sh shell script. Specific subfolders
must be updated and comitted manually.  Art, Sound and Music belong to specific
projects, while Tools is shared across all projects.
